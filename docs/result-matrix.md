# Result Matrix

For each round, compare the player's choice with the computer choice using a single deterministic rule set. Identical choices are draws; otherwise each choice defeats exactly one other choice.